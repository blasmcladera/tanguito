typedef enum {
    OFF,
    MENU,
    ESPERA,
    IZQUIERDA,
    DERECHA,
    ROTAR,
    EYECTAR
} state_t;

int main (void){

    state_t state;
    state = MENU;
    while (1) {
        switch(state){
        case (OFF):
            //APARATO APAGADO, NO SERIA UN ESTADO XD
            break;
        case (MENU){
            if ()
        }



        }

    }
}

